{
    "name": "Hatsu",
    "image": "mcr.microsoft.com/devcontainers/rust:bookworm",
    "postCreateCommand": "sudo apt update && sudo apt install -y mold && cargo install just",
    "customizations": {
        "vscode": {
            "extensions": [
                "rust-lang.rust-analyzer"
            ],
            "settings": {
                "editor.formatOnSave": true
            }
        }
    }
}
